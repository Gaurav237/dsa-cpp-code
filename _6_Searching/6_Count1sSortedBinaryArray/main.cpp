#include <bits/stdc++.h>
using namespace std;

// O(log n) => Time Complexity

// here we need to find floor value of sqrt(k)
// logic: calculate last occurance of number close to sqrt(k),
        //    if not exact square root of a number is possible
int main() {
    int k = 27;
    
    return 0;
}
