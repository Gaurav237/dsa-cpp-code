#include <iostream>
#include <queue>
using namespace std;

int main() {
    /*
    // by default : Maximum Heap is implemented
    priority_queue<int> pq;

//     Minimum Heap
//    priority_queue<int,vector<int>,greater<int>> pq;
    pq.push(10);
    pq.push(15);
    pq.push(5);
    cout << pq.size() << " ";
    cout << pq.top() << " ";

    while(!pq.empty()){
        cout << pq.top() << " ";
        pq.pop();
    }
*/

    /*
    int arr[] = {10,5,15};
    priority_queue<int> pq (arr,arr+3);

    while(!pq.empty()){
        cout << pq.top() << " ";
        pq.pop();
    }
     */

    return 0;
}
